<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.interviewmate.admin.dashboard.mapper.AdminDashboardMapper">

    <select id="countUsers"
            resultType="int">

        SELECT COUNT(*)
        FROM users

    </select>

    <select id="countInterviews"
            resultType="int">

        SELECT COUNT(*)
        FROM interviews

    </select>

    <select id="countAnalysis"
            resultType="int">

        SELECT COUNT(*)
        FROM ai_analysis

    </select>

    <select id="getMonthlyRevenue"
            resultType="int">

        SELECT 0

    </select>

</mapper>