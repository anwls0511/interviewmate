<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.interviewmate.dashboard.mapper.DashboardMapper">

    <select id="countByUserId"
            parameterType="long"
            resultType="int">

        SELECT COUNT(*)
        FROM interviews
        WHERE user_id = #{userId}

    </select>

    <select id="averageScoreByUserId"
            parameterType="long"
            resultType="int">

        SELECT COALESCE(AVG(score), 0)
        FROM interviews
        WHERE user_id = #{userId}

    </select>

    <select id="findRecentByUserId"
            parameterType="long"
            resultType="com.interviewmate.interview.domain.Interview">

        SELECT
            interview_id,
            user_id,
            title,
            job_role,
            career_level,
            tech_stack,
            difficulty,
            question_count,
            status,
            score,
            created_at,
            updated_at
        FROM interviews
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
            LIMIT 5

    </select>

</mapper>